# Local audio

Add optional local files named `angry1.mp3`, `angry2.mp3`, and `angry3.mp3` here before building. The extension works without them; failed playback is caught and the intervention remains usable.
