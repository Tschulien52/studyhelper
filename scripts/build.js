const fs=require('node:fs'),path=require('node:path');const root=path.resolve(__dirname,'..'),src=path.join(root,'src'),out=path.join(root,'dist','chrome');if(process.argv.includes('--clean')){fs.rmSync(path.join(root,'dist'),{recursive:true,force:true});process.exit(0)}fs.rmSync(out,{recursive:true,force:true});function copy(dir){for(const e of fs.readdirSync(dir,{withFileTypes:true})){const from=path.join(dir,e.name),rel=path.relative(src,from),to=path.join(out,rel);if(e.isDirectory()){fs.mkdirSync(to,{recursive:true});copy(from)}else if(!e.name.endsWith('.md')){fs.mkdirSync(path.dirname(to),{recursive:true});fs.copyFileSync(from,to)}}}copy(src);console.log(`Built ${out}`);
